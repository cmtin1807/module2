package kien_thuc.linkedlist;

public class Main {
    /*
    Thêm phần tử:

void add(int index, E element): Thêm phần tử vào vị trí chỉ định.
boolean add(E e): Thêm phần tử vào cuối danh sách.
void addFirst(E e): Thêm phần tử vào đầu danh sách.
void addLast(E e): Thêm phần tử vào cuối danh sách.
Xóa phần tử:

void clear(): Xóa tất cả các phần tử trong danh sách.
E remove(): Xóa và trả về phần tử ở đầu danh sách.
E remove(int index): Xóa và trả về phần tử ở vị trí chỉ định.
boolean remove(Object o): Xóa phần tử đầu tiên có giá trị chỉ định.
E removeFirst(): Xóa và trả về phần tử ở đầu danh sách.
E removeLast(): Xóa và trả về phần tử ở cuối danh sách.
boolean removeFirstOccurrence(Object o): Xóa phần tử đầu tiên có giá trị chỉ định từ đầu danh sách.
boolean removeLastOccurrence(Object o): Xóa phần tử đầu tiên có giá trị chỉ định từ cuối danh sách.
Lấy phần tử:

E get(int index): Lấy phần tử ở vị trí chỉ định.
E getFirst(): Lấy phần tử ở đầu danh sách.
E getLast(): Lấy phần tử ở cuối danh sách.
Thay đổi giá trị phần tử:

E set(int index, E element): Thay đổi phần tử ở vị trí chỉ định.
Kiểm tra và tìm kiếm:

boolean contains(Object o): Kiểm tra xem danh sách có chứa phần tử chỉ định không.
int indexOf(Object o): Trả về chỉ số đầu tiên của phần tử chỉ định trong danh sách.
int lastIndexOf(Object o): Trả về chỉ số cuối cùng của phần tử chỉ định trong danh sách.
boolean isEmpty(): Kiểm tra xem danh sách có rỗng không.
Thông tin về danh sách:

int size(): Trả về số lượng phần tử trong danh sách.
Object[] toArray(): Chuyển danh sách thành mảng các đối tượng.
Duyệt qua các phần tử:

Iterator<E> iterator(): Trả về một iterator để duyệt qua danh sách.
ListIterator<E> listIterator(): Trả về một list iterator để duyệt qua danh sách.
     */
}
